
public enum OverdriveState
{
	Available,
	InProgress,
	Unavailable,
}

public enum OverdriveMode
{
	Multiplier,
	Level
}

public enum OverdriveType
{
	None = 0,
	Movement = 1,
	Sword = 2,
	Magic = 3
}

